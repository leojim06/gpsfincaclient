import { StyledMapDirective } from './styled-map.directive';

describe('StyledMapDirective', () => {
  it('should create an instance', () => {
    const directive = new StyledMapDirective();
    expect(directive).toBeTruthy();
  });
});
